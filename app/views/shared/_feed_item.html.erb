<li	id="#{feed_item.id}">
<span class="user"><%=  link_to gravatar_for(feed_item.user, size: 30), feed_item.user %></span>
	
<span class="content"> <%= feed_item.content %> </span>
<span class="timestamp">Posted by <%= link_to feed_item.user.name, feed_item.user %> <%= time_ago_in_words(feed_item.created_at) %> ago </span> 
</li>
